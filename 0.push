#include<stdio.h>

#include<stdlib.h>

# define size 4

int top=-1 inp-array[size]
void pop()
void pop();
void show();

intmain()
{
    int choice;
    while(1)
    {
    
     printf( " this is the group project done in full combiination of two people in collabolation with each other);
    printf(its almost done with these task);


      switch (choice)      {
      case 1 :push();
     case 2 :pop()
}}}

